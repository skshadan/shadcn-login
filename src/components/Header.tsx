export const Header = () => {
  return (
    <header className="w-full bg-black fixed top-0 left-0" style={{ height: '6rem' }}>
      {/* No text content, just a black bar */}
    </header>
  );
};
